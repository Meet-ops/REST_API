<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <div class="container">
        <h1>Edit Post</h1>
        <div class="form-container">
            <div style="margin-bottom: 1rem; color: var(--text-muted); font-size: 0.9rem;">
                Editing as @<%= post.username %>
            </div>
            <form action="/posts/<%= post.id %>?_method=PATCH" method="POST">
                <textarea name="content" rows="6" required><%= post.content %></textarea>
                <button class="btn btn-large" style="width: 100%;">Update Post</button>
            </form>
        </div>
        <div style="text-align: center; margin-top: 1rem;">
            <a href="/posts" style="color: var(--text-muted);">Cancel and go back</a>
        </div>
    </div>
</body>

</html>